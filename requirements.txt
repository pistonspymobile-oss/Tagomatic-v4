# Minimal requirements for TagOmatic v4.0
# Core dependencies only

PySide6>=6.7
Pillow>=10.4
ollama>=0.1
requests>=2.31.0
onnxruntime>=1.16.0
numpy>=1.24.0

# Optional (only needed if converting .pt models to .onnx):
# ultralytics>=8.0
# torch>=2.0
# torchvision>=0.15
